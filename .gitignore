# Local tooling config — contains the harness project token, never commit/bundle it
.mcp.json

# Output artifacts, not app source
mnt/

# Dev dependencies (installed on demand; never shipped)
dev/node_modules/
dev/package-lock.json

# Python icon generator (dev-only) — icons are pre-generated and committed; keep it out of the bundle
dev/make_icons.py
