{
  "name": "tread-dev",
  "private": true,
  "version": "1.0.0",
  "description": "Dev-only tooling for Tread (smoke tests, icon generation). Not part of the deployed app.",
  "scripts": {
    "test": "node unit-test.js && node smoke-test.js",
    "check": "node check.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
